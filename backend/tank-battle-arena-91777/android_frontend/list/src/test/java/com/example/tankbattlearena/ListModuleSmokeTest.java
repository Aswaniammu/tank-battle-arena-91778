package com.example.tankbattlearena;

import org.junit.Test;
import static org.junit.Assert.assertTrue;

/**
 * Minimal test in the 'list' module to ensure Gradle discovers at least one test
 * across the multi-module build in CI environments.
 */
public class ListModuleSmokeTest {

    @Test
    public void smokeTest() {
        // PUBLIC_INTERFACE
        assertTrue(true);
    }
}
