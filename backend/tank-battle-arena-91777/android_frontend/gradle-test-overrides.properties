# Ensure Gradle/JUnit does not fail the build when no tests are discovered
org.gradle.unsafe.failOnNoMatchingTests=false
# Older test task compatibility flags (some CI wrappers respect these)
test.failOnNoMatchingTests=false
