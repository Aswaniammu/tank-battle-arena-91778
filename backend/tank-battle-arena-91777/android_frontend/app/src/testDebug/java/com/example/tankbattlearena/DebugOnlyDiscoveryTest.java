package com.example.tankbattlearena;

import org.junit.Test;
import static org.junit.Assert.assertTrue;

/**
 * Ensures at least one test exists in the debug unit test variant source set.
 */
public class DebugOnlyDiscoveryTest {
    @Test
    public void discovered() {
        assertTrue(true);
    }
}
