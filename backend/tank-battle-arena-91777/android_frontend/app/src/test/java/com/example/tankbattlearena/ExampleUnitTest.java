package com.example.tankbattlearena;

import static org.junit.Assert.assertEquals;

import org.junit.Test;

/**
 * Simple example unit test to ensure Gradle discovers at least one test.
 */
public class ExampleUnitTest {
    @Test
    public void simple_addition_isCorrect() {
        assertEquals(4, 2 + 2);
    }
}
