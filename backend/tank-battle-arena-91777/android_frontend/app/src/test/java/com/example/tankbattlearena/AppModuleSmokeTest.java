package com.example.tankbattlearena;

import org.junit.Test;
import static org.junit.Assert.assertTrue;

/**
 * Minimal test in the 'app' module to ensure Gradle discovers at least one test
 * for unit test tasks like :app:testDebugUnitTest in CI environments.
 */
public class AppModuleSmokeTest {

    @Test
    public void appModuleSmoke() {
        // PUBLIC_INTERFACE
        assertTrue(true);
    }
}
