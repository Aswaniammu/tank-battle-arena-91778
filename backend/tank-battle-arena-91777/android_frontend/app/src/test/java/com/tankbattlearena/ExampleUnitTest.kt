package com.tankbattlearena

import org.junit.Assert.assertEquals
import org.junit.Test

/**
 * Conventional example unit test to aid Gradle/JUnit discovery in CI.
 */
class ExampleUnitTest {
    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}
