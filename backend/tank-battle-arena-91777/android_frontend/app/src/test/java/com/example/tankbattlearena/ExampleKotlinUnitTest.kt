package com.example.tankbattlearena

import org.junit.Assert.assertEquals
import org.junit.Test

/**
 * Simple Kotlin unit test to ensure test discovery works for Kotlin sources too.
 */
class ExampleKotlinUnitTest {
    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}
