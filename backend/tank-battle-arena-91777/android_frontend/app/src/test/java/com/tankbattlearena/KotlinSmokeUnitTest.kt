package com.tankbattlearena

import org.junit.Assert.assertEquals
import org.junit.Test

/**
 * Minimal Kotlin smoke test to aid Gradle/JUnit discovery across variants.
 */
class KotlinSmokeUnitTest {
    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}
