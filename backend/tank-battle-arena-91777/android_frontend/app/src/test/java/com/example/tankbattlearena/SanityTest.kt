package com.example.tankbattlearena

import org.junit.Assert.assertTrue
import org.junit.Test

/**
 * Simple sanity test to ensure the CI test runner finds at least one test.
 */
class SanityTest {

    @Test
    fun sanity_check() {
        assertTrue(true)
    }
}
