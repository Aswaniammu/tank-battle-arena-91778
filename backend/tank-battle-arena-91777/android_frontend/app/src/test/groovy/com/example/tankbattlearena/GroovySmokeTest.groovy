package com.example.tankbattlearena

import org.junit.Test

import static org.junit.Assert.assertTrue

/**
 * Minimal Groovy-based JUnit4 test to improve discovery on CI where Groovy test patterns are enabled by default.
 */
class GroovySmokeTest {

    @Test
    void groovyTestAlwaysPasses() {
        // PUBLIC_INTERFACE
        assertTrue(true)
    }
}
