package com.example.tankbattlearena

import androidx.test.ext.junit.runners.AndroidJUnit4
import org.junit.Test
import org.junit.runner.RunWith
import org.junit.Assert.assertTrue

/**
 * Minimal instrumented test to ensure at least one test exists for androidTest.
 * Note: This runs on device/emulator; CI may skip running it but its presence helps discovery paths.
 */
@RunWith(AndroidJUnit4::class)
class InstrumentedDummyTest {
    @Test
    fun instrumentedAlwaysPasses() {
        // PUBLIC_INTERFACE
        assertTrue(true)
    }
}
