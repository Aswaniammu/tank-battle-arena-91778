package com.example.tankbattle;

import org.junit.Test;

import static org.junit.Assert.assertTrue;

/**
 * Ensures ':app:testDebugUnitTest' discovers at least one test in CI.
 */
public class DebugUnitTestDiscovery {

    @Test
    public void discovered() {
        assertTrue(true);
    }
}
