tasks.matching { it.name == "testDebugUnitTest" }.configureEach {
    it.setFailOnNoTest(false)
    it.failOnNoMatchingTests = false
}
apply from: "${project.projectDir}/gradle.testtask.config.gradle"
