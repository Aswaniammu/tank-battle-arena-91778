# This file helps Gradle locate the Android SDK in CI environments.
# If ANDROID_SDK_ROOT is set in CI, Gradle will pick it up automatically.
# Otherwise update this path accordingly.
sdk.dir=${ANDROID_SDK_ROOT}
