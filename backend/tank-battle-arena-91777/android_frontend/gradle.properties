# Global Gradle properties for CI stability
org.gradle.console=plain
org.gradle.warning.mode=all
# Do not fail when no tests are discovered (applies to all Test tasks)
org.gradle.test.failOnNoTests=false
# Quiet down configuration on demand warnings
org.gradle.configureondemand=false
